# Base URL of the backend's /api prefix (see backend/src/main.ts -> setGlobalPrefix('api')).
# Your backend is currently running locally on port 4000, so during dev this is already the default
# in lib/api.ts if you don't set it. Override for staging/production once the backend is deployed.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
